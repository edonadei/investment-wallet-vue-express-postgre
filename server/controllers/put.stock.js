export default function putStocks(req, res) {
    // NOTE: remove the "res.status(501).send({ message: 'not implemented' }})"
    //       to make it work
    res.status(501).send({ message: 'not implemented' })
  }
  